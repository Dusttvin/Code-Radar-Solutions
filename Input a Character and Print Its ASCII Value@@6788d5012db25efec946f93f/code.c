#include <stdio.h>

int main() {
    char ch;

    // Read a single character from the user
    scanf("%c", &ch);

    // Print the ASCII value of the character
    printf("ASCII Value: %d\n", ch);

    return 0;
}