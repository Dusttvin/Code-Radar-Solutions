#include <stdio.h>

int main() {
    int num1, num2;

    // Read two integers from the user
    scanf("%d %d", &num1, &num2);

    // Calculate the sum
    int sum = num1 + num2;

    // Print the sum
    printf("Sum: %d\n", sum);

    return 0;
}